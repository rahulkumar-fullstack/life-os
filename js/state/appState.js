// Central object that stores the entire app data
//-----------------------------------------------
export const appState = {
  // Stores all user tasks
  tasks: [],

  // Stores user notes
  notes: [],

  // Stores habit tracking data
  habits: [],

  // Stores user preferences and settings
  preferences: {}
};
